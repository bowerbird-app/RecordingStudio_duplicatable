<div class="mx-auto flex w-full max-w-6xl flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Duplicatable Demo",
    subtitle: "This is a demo of the duplication functionality",
    variant: :h1
  ) %>

  <% if flash[:notice].present? %>
    <%= render FlatPack::Alert::Component.new(
      title: "Duplication complete",
      description: flash[:notice],
      style: :success
    ) %>
  <% elsif flash[:alert].present? %>
    <%= render FlatPack::Alert::Component.new(
      title: "Duplication error",
      description: flash[:alert],
      style: :danger
    ) %>
  <% end %>

  <div class="grid gap-6 md:grid-cols-2 xl:grid-cols-3">
    <% @pages.each do |page| %>
      <%= render FlatPack::Card::Component.new(style: :outlined) do |card| %>
        <% card.body do %>
          <div class="flex flex-col items-start gap-2">
            <h3 class="text-lg font-semibold text-(--surface-content-color)"><%= page.title %></h3>

            <%= render FlatPack::Badge::Component.new(
              text: "Page",
              style: :info,
              size: :sm
            ) %>

            <%= form_with url: duplicate_page_path(page.slug), method: :post, local: true do %>
              <%= render FlatPack::Button::Component.new(
                text: "Duplicate",
                style: :default,
                size: :sm,
                type: "submit"
              ) %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>
